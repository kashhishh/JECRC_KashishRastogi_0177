import { ClickBlockDirective } from './click-block';

describe('ClickBlockDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickBlockDirective();
    expect(directive).toBeTruthy();
  });
});